# 禅妖山海行 - 客户端环境配置
# 复制本文件为项目根目录的 .env（或 config.env）后按需修改
# .env / .env.local 不会提交到 Git
#
# 加载优先级（后者覆盖前者）:
#   res://.env → res://.env.local → res://config.env → user://.env

# 后端 API 根地址（不要末尾斜杠）
API_BASE=http://localhost:8080
